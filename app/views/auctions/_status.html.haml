-if @product.auction.top_bid.user == current_user
  %p You won the auction!
  =link_to 'claim it', transfer_product_path(@product), class: 'btn btn-primary', method: :put
-else
  %p You lost the auctiion.
